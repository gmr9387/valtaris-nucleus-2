import { t as Route } from "./_app.workflows.runs._runId-DVKYLZBJ.js";
import { a as Panel, i as PageHeader, r as PageBody, s as StatusPill, t as EmptyState } from "./platform-ui-8S9LjWBS.js";
import { t as LoadingState } from "./LoadingState-CXQD3J30.js";
import { t as ErrorState } from "./ErrorState-C6IYvl2K.js";
import { a as useWorkflowSteps, n as useWorkflowAuditEvents, r as useWorkflowRun } from "./queries-B99uDuNs.js";
import { c as failWorkflow, f as PermissionGate, i as completeWorkflow, n as cancelWorkflow, r as completeStep, s as failStep, u as startStep } from "./runtime-C8NsL7P9.js";
import { useState } from "react";
import { Link } from "@tanstack/react-router";
import { Fragment, jsx, jsxs } from "react/jsx-runtime";
import { useQueryClient } from "@tanstack/react-query";
import { ArrowLeft, Ban, CheckCircle2, XCircle } from "lucide-react";
//#region src/routes/_app.workflows.runs.$runId.tsx?tsr-split=component
function RunDetailPage() {
	const { runId } = Route.useParams();
	const qc = useQueryClient();
	const run = useWorkflowRun(runId);
	const steps = useWorkflowSteps(runId);
	const audit = useWorkflowAuditEvents({
		run_id: runId,
		limit: 200
	});
	const [stepKey, setStepKey] = useState("");
	const [busy, setBusy] = useState(false);
	const [error, setError] = useState(null);
	const orgId = run.data?.organization_id ?? null;
	const isTerminal = run.data?.status === "completed" || run.data?.status === "failed" || run.data?.status === "cancelled";
	async function withBusy(fn) {
		setBusy(true);
		setError(null);
		try {
			await fn();
		} catch (err) {
			setError(err instanceof Error ? err.message : "Action failed");
		} finally {
			setBusy(false);
		}
	}
	async function invalidate() {
		await Promise.all([
			qc.invalidateQueries({ queryKey: ["workflow-run", runId] }),
			qc.invalidateQueries({ queryKey: ["workflow-steps", runId] }),
			qc.invalidateQueries({ queryKey: [
				"workflow-audit",
				void 0,
				void 0,
				runId,
				200
			] })
		]);
	}
	if (run.isLoading) return /* @__PURE__ */ jsx(PageBody, { children: /* @__PURE__ */ jsx(LoadingState, {}) });
	if (run.error) return /* @__PURE__ */ jsx(PageBody, { children: /* @__PURE__ */ jsx(ErrorState, {
		error: run.error,
		onRetry: () => void run.refetch()
	}) });
	if (!run.data) return /* @__PURE__ */ jsx(PageBody, { children: /* @__PURE__ */ jsx(EmptyState, { title: "Run not found" }) });
	const r = run.data;
	return /* @__PURE__ */ jsxs(Fragment, { children: [/* @__PURE__ */ jsx(PageHeader, {
		eyebrow: "RUN",
		title: `Run ${r.id.slice(0, 8)}`,
		description: `Workflow ${r.workflow_id.slice(0, 8)} · version ${r.version_id.slice(0, 8)}`,
		actions: /* @__PURE__ */ jsxs("div", {
			className: "flex items-center gap-2",
			children: [/* @__PURE__ */ jsxs(Link, {
				to: "/workflows/$workflowId/runs",
				params: { workflowId: r.workflow_id },
				className: "inline-flex h-8 items-center gap-1.5 rounded-md border border-border bg-surface-2 px-3 text-xs",
				children: [/* @__PURE__ */ jsx(ArrowLeft, { className: "h-3.5 w-3.5" }), "Runs"]
			}), !isTerminal && /* @__PURE__ */ jsxs(Fragment, { children: [/* @__PURE__ */ jsxs(PermissionGate, {
				permission: "workflows:run",
				children: [/* @__PURE__ */ jsxs("button", {
					onClick: () => withBusy(async () => {
						await completeWorkflow(orgId, r.id);
						await invalidate();
					}),
					disabled: busy,
					className: "inline-flex h-8 items-center gap-1.5 rounded-md bg-status-paid/20 px-3 text-xs text-status-paid disabled:opacity-50",
					children: [/* @__PURE__ */ jsx(CheckCircle2, { className: "h-3.5 w-3.5" }), "Complete"]
				}), /* @__PURE__ */ jsxs("button", {
					onClick: () => withBusy(async () => {
						await failWorkflow(orgId, r.id, { message: "Marked failed from console" });
						await invalidate();
					}),
					disabled: busy,
					className: "inline-flex h-8 items-center gap-1.5 rounded-md bg-status-denied/20 px-3 text-xs text-status-denied disabled:opacity-50",
					children: [/* @__PURE__ */ jsx(XCircle, { className: "h-3.5 w-3.5" }), "Fail"]
				})]
			}), /* @__PURE__ */ jsx(PermissionGate, {
				permission: "workflows:cancel",
				children: /* @__PURE__ */ jsxs("button", {
					onClick: () => withBusy(async () => {
						await cancelWorkflow(orgId, r.id, "Cancelled from console");
						await invalidate();
					}),
					disabled: busy,
					className: "inline-flex h-8 items-center gap-1.5 rounded-md border border-border bg-surface-2 px-3 text-xs disabled:opacity-50",
					children: [/* @__PURE__ */ jsx(Ban, { className: "h-3.5 w-3.5" }), "Cancel"]
				})
			})] })]
		})
	}), /* @__PURE__ */ jsx(PageBody, { children: /* @__PURE__ */ jsxs("div", {
		className: "space-y-6",
		children: [
			/* @__PURE__ */ jsxs("div", {
				className: "flex items-center gap-3 text-xs text-muted-foreground",
				children: [
					/* @__PURE__ */ jsx(StatusPill, { status: r.status }),
					/* @__PURE__ */ jsxs("span", { children: ["Started ", r.started_at ? new Date(r.started_at).toLocaleString() : "—"] }),
					/* @__PURE__ */ jsxs("span", { children: ["Completed ", r.completed_at ? new Date(r.completed_at).toLocaleString() : "—"] })
				]
			}),
			error && /* @__PURE__ */ jsx("div", {
				className: "rounded-md border border-destructive/30 bg-destructive/5 p-3 text-xs text-destructive",
				children: error
			}),
			/* @__PURE__ */ jsxs(Panel, {
				title: "Steps",
				description: "Track individual step execution.",
				children: [!isTerminal && /* @__PURE__ */ jsx(PermissionGate, {
					permission: "workflows:run",
					children: /* @__PURE__ */ jsxs("div", {
						className: "mb-3 flex gap-2",
						children: [/* @__PURE__ */ jsx("input", {
							value: stepKey,
							onChange: (e) => setStepKey(e.target.value),
							placeholder: "step_key",
							className: "h-8 flex-1 rounded-md border border-border bg-surface-2 px-2 font-mono text-xs"
						}), /* @__PURE__ */ jsx("button", {
							disabled: busy || !stepKey.trim(),
							onClick: () => withBusy(async () => {
								await startStep({
									run_id: r.id,
									step_key: stepKey.trim()
								});
								setStepKey("");
								await invalidate();
							}),
							className: "inline-flex h-8 items-center rounded-md bg-primary px-3 text-xs font-medium text-primary-foreground disabled:opacity-50",
							children: "Start step"
						})]
					})
				}), steps.isLoading ? /* @__PURE__ */ jsx(LoadingState, { rows: 2 }) : (steps.data ?? []).length === 0 ? /* @__PURE__ */ jsx(EmptyState, { title: "No steps yet" }) : /* @__PURE__ */ jsx("div", {
					className: "divide-y divide-border",
					children: (steps.data ?? []).map((s) => /* @__PURE__ */ jsxs("div", {
						className: "grid grid-cols-[1fr_120px_160px_220px] items-center gap-3 py-3 text-sm",
						children: [
							/* @__PURE__ */ jsx("div", {
								className: "font-mono text-xs",
								children: s.step_key
							}),
							/* @__PURE__ */ jsx("div", { children: /* @__PURE__ */ jsx(StatusPill, { status: s.status }) }),
							/* @__PURE__ */ jsx("div", {
								className: "text-xs text-muted-foreground",
								children: s.started_at ? new Date(s.started_at).toLocaleString() : "—"
							}),
							/* @__PURE__ */ jsx("div", {
								className: "flex justify-end gap-2",
								children: s.status === "running" && !isTerminal && /* @__PURE__ */ jsxs(PermissionGate, {
									permission: "workflows:run",
									children: [/* @__PURE__ */ jsx("button", {
										disabled: busy,
										onClick: () => withBusy(async () => {
											await completeStep({
												run_id: r.id,
												step_key: s.step_key
											});
											await invalidate();
										}),
										className: "inline-flex h-7 items-center rounded-md border border-border bg-surface-2 px-2 text-xs",
										children: "Complete"
									}), /* @__PURE__ */ jsx("button", {
										disabled: busy,
										onClick: () => withBusy(async () => {
											await failStep({
												run_id: r.id,
												step_key: s.step_key,
												error: { message: "Marked failed" }
											});
											await invalidate();
										}),
										className: "inline-flex h-7 items-center rounded-md border border-border bg-surface-2 px-2 text-xs",
										children: "Fail"
									})]
								})
							})
						]
					}, s.id))
				})]
			}),
			/* @__PURE__ */ jsx(Panel, {
				title: "Audit trail",
				description: "Every runtime transition emits an event.",
				children: audit.isLoading ? /* @__PURE__ */ jsx(LoadingState, { rows: 2 }) : (audit.data ?? []).length === 0 ? /* @__PURE__ */ jsx(EmptyState, { title: "No audit events yet" }) : /* @__PURE__ */ jsx("div", {
					className: "divide-y divide-border",
					children: (audit.data ?? []).map((e) => /* @__PURE__ */ jsxs("div", {
						className: "grid grid-cols-[180px_1fr_180px] items-center gap-3 py-2 text-xs",
						children: [
							/* @__PURE__ */ jsx("div", {
								className: "font-mono text-foreground",
								children: e.event_type
							}),
							/* @__PURE__ */ jsx("div", {
								className: "truncate text-muted-foreground",
								children: e.payload ? JSON.stringify(e.payload) : "—"
							}),
							/* @__PURE__ */ jsx("div", {
								className: "text-right text-muted-foreground",
								children: new Date(e.occurred_at).toLocaleString()
							})
						]
					}, e.id))
				})
			})
		]
	}) })] });
}
//#endregion
export { RunDetailPage as component };
