import { t as supabase } from "./client-CEGIMAqI.js";
import { n as useAuth } from "./auth-context-BNAOXFcn.js";
import { a as Th, i as Td, n as FieldStyles, t as Field } from "./_app.organizations-C167SJ8c.js";
import { d as useEnvironments, f as useMyOrgMembership, h as useProjects, n as canManageProjects } from "./queries-CytnrJBF.js";
import { t as useOrgStore } from "./org-store-DHMPJw-C.js";
import { i as PageHeader, o as StatusDot, r as PageBody, t as EmptyState } from "./platform-ui-8S9LjWBS.js";
import { n as logAudit, t as createCorrelationId } from "./audit-CST1_a7C.js";
import { useEffect, useState } from "react";
import { Fragment, jsx, jsxs } from "react/jsx-runtime";
import { z } from "zod";
import { useMutation, useQueryClient } from "@tanstack/react-query";
import { toast } from "sonner";
import { Plus } from "lucide-react";
//#region src/routes/_app.environments.tsx?tsr-split=component
var schema = z.object({
	project_id: z.string().uuid(),
	name: z.string().trim().min(1).max(60),
	env_type: z.enum([
		"development",
		"staging",
		"production"
	])
});
function EnvsPage() {
	const { currentOrgId } = useOrgStore();
	const { user } = useAuth();
	const envs = useEnvironments(currentOrgId);
	const projects = useProjects(currentOrgId);
	const membership = useMyOrgMembership(currentOrgId);
	const canCreate = canManageProjects(membership.data?.role);
	const qc = useQueryClient();
	const [open, setOpen] = useState(false);
	const [projectId, setProjectId] = useState("");
	const [name, setName] = useState("");
	const [envType, setEnvType] = useState("development");
	useEffect(() => {
		if (!projectId && projects.data?.[0]?.id) setProjectId(projects.data[0].id);
	}, [projectId, projects.data]);
	const create = useMutation({
		mutationFn: async () => {
			if (!currentOrgId) throw new Error("Select an organization first.");
			if (!user) throw new Error("You must be signed in.");
			if (!canCreate) throw new Error("You do not have permission to create environments.");
			const parsed = schema.parse({
				project_id: projectId,
				name,
				env_type: envType
			});
			const correlationId = createCorrelationId();
			const { data, error } = await supabase.from("environments").insert({
				project_id: parsed.project_id,
				name: parsed.name,
				env_type: parsed.env_type,
				created_by: user.id
			}).select().single();
			if (error) throw error;
			await logAudit({
				organization_id: currentOrgId,
				module: "tenancy",
				entity_type: "environment",
				entity_id: data.id,
				action: "create",
				after: data,
				correlation_id: correlationId
			});
			return data;
		},
		onSuccess: () => {
			toast.success("Environment created");
			setName("");
			setOpen(false);
			qc.invalidateQueries({ queryKey: ["envs", currentOrgId] });
			qc.invalidateQueries({ queryKey: ["project-envs", projectId] });
			qc.invalidateQueries({ queryKey: ["audit", currentOrgId] });
		},
		onError: (error) => {
			toast.error(error.message);
		}
	});
	return /* @__PURE__ */ jsxs(Fragment, { children: [/* @__PURE__ */ jsx(PageHeader, {
		eyebrow: "TENANCY",
		title: "Environments",
		description: "Isolated runtime targets per project: development, staging, and production.",
		actions: /* @__PURE__ */ jsxs("button", {
			onClick: () => setOpen((v) => !v),
			disabled: !currentOrgId || !projects.data?.length || !canCreate,
			className: "inline-flex h-9 items-center gap-1.5 rounded-md bg-primary px-3 text-sm font-medium text-primary-foreground hover:opacity-90 disabled:opacity-50",
			title: !canCreate ? "Owner, admin, or manager role required" : void 0,
			children: [/* @__PURE__ */ jsx(Plus, { className: "h-3.5 w-3.5" }), "New environment"]
		})
	}), /* @__PURE__ */ jsxs(PageBody, { children: [
		!currentOrgId && /* @__PURE__ */ jsx(EmptyState, {
			title: "Select an organization",
			description: "Choose or create an organization before creating environments."
		}),
		currentOrgId && open && /* @__PURE__ */ jsxs("form", {
			onSubmit: (event) => {
				event.preventDefault();
				create.mutate();
			},
			className: "mb-6 rounded-lg border border-border bg-surface-1 p-4",
			children: [/* @__PURE__ */ jsxs("div", {
				className: "grid grid-cols-1 gap-3 md:grid-cols-3",
				children: [
					/* @__PURE__ */ jsx(Field, {
						label: "Project",
						children: /* @__PURE__ */ jsxs("select", {
							className: "input",
							value: projectId,
							onChange: (event) => setProjectId(event.target.value),
							required: true,
							children: [/* @__PURE__ */ jsx("option", {
								value: "",
								children: "Select project…"
							}), projects.data?.map((project) => /* @__PURE__ */ jsx("option", {
								value: project.id,
								children: project.name
							}, project.id))]
						})
					}),
					/* @__PURE__ */ jsx(Field, {
						label: "Name",
						children: /* @__PURE__ */ jsx("input", {
							className: "input",
							value: name,
							onChange: (event) => setName(event.target.value),
							required: true,
							maxLength: 60,
							placeholder: "Production"
						})
					}),
					/* @__PURE__ */ jsx(Field, {
						label: "Type",
						children: /* @__PURE__ */ jsxs("select", {
							className: "input",
							value: envType,
							onChange: (event) => setEnvType(event.target.value),
							children: [
								/* @__PURE__ */ jsx("option", {
									value: "development",
									children: "development"
								}),
								/* @__PURE__ */ jsx("option", {
									value: "staging",
									children: "staging"
								}),
								/* @__PURE__ */ jsx("option", {
									value: "production",
									children: "production"
								})
							]
						})
					})
				]
			}), /* @__PURE__ */ jsxs("div", {
				className: "mt-3 flex justify-end gap-2",
				children: [/* @__PURE__ */ jsx("button", {
					type: "button",
					onClick: () => setOpen(false),
					className: "h-9 rounded-md border border-border bg-surface-2 px-3 text-sm hover:bg-surface-3",
					children: "Cancel"
				}), /* @__PURE__ */ jsx("button", {
					type: "submit",
					disabled: create.isPending || !canCreate,
					className: "h-9 rounded-md bg-primary px-3 text-sm font-medium text-primary-foreground hover:opacity-90 disabled:opacity-50",
					children: create.isPending ? "Creating…" : "Create"
				})]
			})]
		}),
		currentOrgId && envs.data?.length ? /* @__PURE__ */ jsx("div", {
			className: "overflow-hidden rounded-lg border border-border",
			children: /* @__PURE__ */ jsxs("table", {
				className: "w-full text-sm",
				children: [/* @__PURE__ */ jsx("thead", {
					className: "bg-surface-1 text-mono-xs text-muted-foreground",
					children: /* @__PURE__ */ jsxs("tr", { children: [
						/* @__PURE__ */ jsx(Th, { children: "Project" }),
						/* @__PURE__ */ jsx(Th, { children: "Name" }),
						/* @__PURE__ */ jsx(Th, { children: "Type" }),
						/* @__PURE__ */ jsx(Th, { children: "Created" })
					] })
				}), /* @__PURE__ */ jsx("tbody", {
					className: "divide-y divide-border bg-surface-1/40",
					children: envs.data.map((env) => /* @__PURE__ */ jsxs("tr", {
						className: "hover:bg-surface-2/60",
						children: [
							/* @__PURE__ */ jsx(Td, { children: /* @__PURE__ */ jsx("span", {
								className: "font-medium",
								children: env.project_name ?? "Unknown project"
							}) }),
							/* @__PURE__ */ jsx(Td, { children: /* @__PURE__ */ jsx("span", {
								className: "font-mono text-xs",
								children: env.name
							}) }),
							/* @__PURE__ */ jsx(Td, { children: /* @__PURE__ */ jsxs("span", {
								className: "inline-flex items-center gap-1.5",
								children: [/* @__PURE__ */ jsx(StatusDot, { status: env.env_type }), /* @__PURE__ */ jsx("span", {
									className: "capitalize",
									children: env.env_type
								})]
							}) }),
							/* @__PURE__ */ jsx(Td, { children: /* @__PURE__ */ jsx("span", {
								className: "text-muted-foreground",
								children: new Date(env.created_at).toLocaleDateString()
							}) })
						]
					}, env.id))
				})]
			})
		}) : currentOrgId ? /* @__PURE__ */ jsx(EmptyState, {
			title: "No environments yet",
			description: projects.data?.length ? canCreate ? "Create one for an existing project." : "No environments are available yet." : "Create a project first."
		}) : null,
		/* @__PURE__ */ jsx(FieldStyles, {})
	] })] });
}
//#endregion
export { EnvsPage as component };
