import { t as Route } from "./_app.workflows._workflowId-CTOldeY8.js";
import { a as Panel, i as PageHeader, r as PageBody, s as StatusPill, t as EmptyState } from "./platform-ui-8S9LjWBS.js";
import { t as LoadingState } from "./LoadingState-CXQD3J30.js";
import { t as ErrorState } from "./ErrorState-C6IYvl2K.js";
import { o as useWorkflowVersions, t as useWorkflow } from "./queries-B99uDuNs.js";
import { a as createDraftVersion, d as startWorkflow, f as PermissionGate, l as publishVersion, t as archiveWorkflow } from "./runtime-C8NsL7P9.js";
import { useState } from "react";
import { Link } from "@tanstack/react-router";
import { Fragment, jsx, jsxs } from "react/jsx-runtime";
import { z } from "zod";
import { useQueryClient } from "@tanstack/react-query";
import { Archive, ArrowLeft, GitBranch, Play } from "lucide-react";
//#region src/lib/schemas/workflows.schemas.ts
var workflowStatusSchema = z.enum([
	"draft",
	"active",
	"archived"
]);
z.enum([
	"draft",
	"published",
	"archived"
]);
z.enum([
	"pending",
	"running",
	"completed",
	"failed",
	"cancelled"
]);
z.enum([
	"pending",
	"running",
	"completed",
	"failed",
	"skipped"
]);
z.object({
	organization_id: z.string().uuid(),
	name: z.string().min(1).max(120),
	description: z.string().max(1e3).optional().nullable()
});
z.object({
	id: z.string().uuid(),
	name: z.string().min(1).max(120).optional(),
	description: z.string().max(1e3).optional().nullable(),
	status: workflowStatusSchema.optional()
});
var workflowDefinitionSchema = z.object({ steps: z.array(z.object({
	key: z.string().min(1).max(64).regex(/^[a-zA-Z0-9_.-]+$/),
	label: z.string().min(1).max(120),
	kind: z.string().min(1).max(64).default("task"),
	config: z.record(z.string(), z.unknown()).optional()
})).min(1).max(200) });
z.object({
	workflow_id: z.string().uuid(),
	definition: workflowDefinitionSchema
});
z.object({
	workflow_id: z.string().uuid(),
	version_id: z.string().uuid(),
	input: z.unknown().optional()
});
//#endregion
//#region src/routes/_app.workflows.$workflowId.tsx?tsr-split=component
var SAMPLE_DEFINITION = JSON.stringify({ steps: [
	{
		key: "ingest",
		label: "Ingest input",
		kind: "task"
	},
	{
		key: "transform",
		label: "Transform payload",
		kind: "task"
	},
	{
		key: "emit",
		label: "Emit result",
		kind: "task"
	}
] }, null, 2);
function WorkflowDetailPage() {
	const { workflowId } = Route.useParams();
	const navigate = Route.useNavigate();
	const qc = useQueryClient();
	const workflow = useWorkflow(workflowId);
	const versions = useWorkflowVersions(workflowId);
	const [defText, setDefText] = useState(SAMPLE_DEFINITION);
	const [busy, setBusy] = useState(false);
	const [error, setError] = useState(null);
	const orgId = workflow.data?.organization_id ?? null;
	const latestPublished = versions.data?.find((v) => v.status === "published") ?? null;
	async function handleDraft() {
		if (!workflow.data) return;
		setBusy(true);
		setError(null);
		try {
			const parsed = workflowDefinitionSchema.parse(JSON.parse(defText));
			await createDraftVersion({
				workflow_id: workflow.data.id,
				organization_id: workflow.data.organization_id,
				definition: parsed
			});
			await qc.invalidateQueries({ queryKey: ["workflow-versions", workflowId] });
		} catch (err) {
			setError(err instanceof Error ? err.message : "Failed to draft version");
		} finally {
			setBusy(false);
		}
	}
	async function handlePublish(versionId) {
		if (!workflow.data) return;
		setBusy(true);
		setError(null);
		try {
			await publishVersion({
				version_id: versionId,
				workflow_id: workflow.data.id,
				organization_id: workflow.data.organization_id
			});
			await qc.invalidateQueries({ queryKey: ["workflow-versions", workflowId] });
			await qc.invalidateQueries({ queryKey: ["workflow", workflowId] });
		} catch (err) {
			setError(err instanceof Error ? err.message : "Failed to publish");
		} finally {
			setBusy(false);
		}
	}
	async function handleArchive() {
		if (!workflow.data) return;
		setBusy(true);
		try {
			await archiveWorkflow({
				workflow_id: workflow.data.id,
				organization_id: workflow.data.organization_id
			});
			await qc.invalidateQueries({ queryKey: ["workflow", workflowId] });
			await qc.invalidateQueries({ queryKey: ["workflows", orgId] });
		} finally {
			setBusy(false);
		}
	}
	async function handleStart() {
		if (!workflow.data || !latestPublished) return;
		setBusy(true);
		setError(null);
		try {
			const run = await startWorkflow({
				organization_id: workflow.data.organization_id,
				workflow_id: workflow.data.id,
				version_id: latestPublished.id
			});
			navigate({
				to: "/workflows/runs/$runId",
				params: { runId: run.id }
			});
		} catch (err) {
			setError(err instanceof Error ? err.message : "Failed to start run");
		} finally {
			setBusy(false);
		}
	}
	if (workflow.isLoading) return /* @__PURE__ */ jsx(PageBody, { children: /* @__PURE__ */ jsx(LoadingState, {}) });
	if (workflow.error) return /* @__PURE__ */ jsx(PageBody, { children: /* @__PURE__ */ jsx(ErrorState, {
		error: workflow.error,
		onRetry: () => void workflow.refetch()
	}) });
	if (!workflow.data) return /* @__PURE__ */ jsx(PageBody, { children: /* @__PURE__ */ jsx(EmptyState, { title: "Workflow not found" }) });
	const wf = workflow.data;
	return /* @__PURE__ */ jsxs(Fragment, { children: [/* @__PURE__ */ jsx(PageHeader, {
		eyebrow: "WORKFLOW",
		title: wf.name,
		description: wf.description ?? void 0,
		actions: /* @__PURE__ */ jsxs("div", {
			className: "flex items-center gap-2",
			children: [
				/* @__PURE__ */ jsxs(Link, {
					to: "/workflows",
					className: "inline-flex h-8 items-center gap-1.5 rounded-md border border-border bg-surface-2 px-3 text-xs",
					children: [/* @__PURE__ */ jsx(ArrowLeft, { className: "h-3.5 w-3.5" }), " Back"]
				}),
				/* @__PURE__ */ jsx(Link, {
					to: "/workflows/$workflowId/runs",
					params: { workflowId: wf.id },
					className: "inline-flex h-8 items-center rounded-md border border-border bg-surface-2 px-3 text-xs",
					children: "Runs"
				}),
				/* @__PURE__ */ jsx(PermissionGate, {
					permission: "workflows:run",
					children: /* @__PURE__ */ jsxs("button", {
						onClick: handleStart,
						disabled: !latestPublished || busy || wf.status === "archived",
						className: "inline-flex h-8 items-center gap-1.5 rounded-md bg-primary px-3 text-xs font-medium text-primary-foreground disabled:opacity-50",
						children: [/* @__PURE__ */ jsx(Play, { className: "h-3.5 w-3.5" }), "Start run"]
					})
				}),
				/* @__PURE__ */ jsx(PermissionGate, {
					permission: "workflows:admin",
					children: /* @__PURE__ */ jsxs("button", {
						onClick: handleArchive,
						disabled: busy || wf.status === "archived",
						className: "inline-flex h-8 items-center gap-1.5 rounded-md border border-border bg-surface-2 px-3 text-xs disabled:opacity-50",
						children: [/* @__PURE__ */ jsx(Archive, { className: "h-3.5 w-3.5" }), "Archive"]
					})
				})
			]
		})
	}), /* @__PURE__ */ jsx(PageBody, { children: /* @__PURE__ */ jsxs("div", {
		className: "space-y-6",
		children: [
			/* @__PURE__ */ jsxs("div", {
				className: "flex items-center gap-3 text-xs text-muted-foreground",
				children: [/* @__PURE__ */ jsx(StatusPill, { status: wf.status }), /* @__PURE__ */ jsxs("span", { children: ["Created ", new Date(wf.created_at).toLocaleString()] })]
			}),
			error && /* @__PURE__ */ jsx("div", {
				className: "rounded-md border border-destructive/30 bg-destructive/5 p-3 text-xs text-destructive",
				children: error
			}),
			/* @__PURE__ */ jsx(PermissionGate, {
				permission: "workflows:update",
				children: /* @__PURE__ */ jsx(Panel, {
					title: "Draft a new version",
					description: "Define ordered steps. Published versions are immutable.",
					icon: /* @__PURE__ */ jsx(GitBranch, { className: "h-4 w-4" }),
					children: /* @__PURE__ */ jsxs("div", {
						className: "space-y-3",
						children: [/* @__PURE__ */ jsx("textarea", {
							value: defText,
							onChange: (e) => setDefText(e.target.value),
							rows: 10,
							spellCheck: false,
							className: "block w-full rounded-md border border-border bg-surface-2 px-3 py-2 font-mono text-xs"
						}), /* @__PURE__ */ jsx("button", {
							onClick: handleDraft,
							disabled: busy,
							className: "inline-flex h-8 items-center rounded-md bg-primary px-3 text-xs font-medium text-primary-foreground disabled:opacity-50",
							children: busy ? "Working…" : "Create draft version"
						})]
					})
				})
			}),
			/* @__PURE__ */ jsx(Panel, {
				title: "Versions",
				description: "Drafts can be edited; published versions are frozen.",
				children: versions.isLoading ? /* @__PURE__ */ jsx(LoadingState, { rows: 2 }) : (versions.data ?? []).length === 0 ? /* @__PURE__ */ jsx(EmptyState, {
					title: "No versions",
					description: "Draft the first version above."
				}) : /* @__PURE__ */ jsx("div", {
					className: "divide-y divide-border",
					children: (versions.data ?? []).map((v) => /* @__PURE__ */ jsxs("div", {
						className: "grid grid-cols-[80px_120px_1fr_160px] items-center gap-3 py-3 text-sm",
						children: [
							/* @__PURE__ */ jsxs("div", {
								className: "font-mono text-xs",
								children: ["v", v.version_number]
							}),
							/* @__PURE__ */ jsx(StatusPill, { status: v.status }),
							/* @__PURE__ */ jsxs("div", {
								className: "text-xs text-muted-foreground",
								children: [
									"Created ",
									new Date(v.created_at).toLocaleString(),
									v.published_at && ` · Published ${new Date(v.published_at).toLocaleString()}`
								]
							}),
							/* @__PURE__ */ jsx("div", {
								className: "text-right",
								children: v.status === "draft" && /* @__PURE__ */ jsx(PermissionGate, {
									permission: "workflows:publish",
									children: /* @__PURE__ */ jsx("button", {
										onClick: () => handlePublish(v.id),
										disabled: busy,
										className: "inline-flex h-7 items-center rounded-md border border-border bg-surface-2 px-2 text-xs disabled:opacity-50",
										children: "Publish"
									})
								})
							})
						]
					}, v.id))
				})
			})
		]
	}) })] });
}
//#endregion
export { WorkflowDetailPage as component };
